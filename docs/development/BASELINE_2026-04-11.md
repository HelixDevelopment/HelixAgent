# Remediation Baseline — 2026-04-11

Snapshot taken before executing the 9-phase remediation plan. Used for regression
comparison at Phase 9 sign-off.

## Git state
- HEAD: `97345b4742ea680a5bbc46c8f926ed5c70ed87e8`
- Branch: `main`
- Submodules: 150 total, all in sync (no `-` or `+` prefix)
- Untracked: `MCP-Servers/` (unrelated dir)
- Modified: `CLAUDE.md` (from the earlier /init pass — suggestions applied)

## Toolchain
- Go: `go1.26.1-X:nodwarf5 linux/amd64`
- Resource limits in effect for all test/build runs: `GOMAXPROCS=2 nice -n 19`

## Baseline build / vet
| Check | Target | Result |
|---|---|---|
| `go build` | `./cmd/helixagent` | **PASS** |
| `go vet` | `./internal/...` | **PASS** |
| `go vet` | `./tests/providers/...` | **FAIL** — `undefined: llm.Client` (102 refs across 9 files, dead since earlier refactor) |

## Known-broken / dead entries going into Phase 1
1. `internal/routing/semantic/` — zero Go importers; only README and docs references.
2. `internal/routing/` — contains only the above + README; whole subtree is dead.
3. `internal/ide/lsp/` — empty directory (no Go files).
4. `internal/ide/` — contains only the empty `lsp/` subdir; whole subtree is dead.
5. `tests/providers/*.go` — 9 files, 102 references to removed `llm.Client` API. Not
   compiled, not wired into any Makefile target. Dead.
6. Backup files (to be deleted):
   - `internal/agents/subagent/manager.go.bak`
   - `internal/agents/subagent/orchestrator.go.bak`
   - `internal/mcp/snowcli_adapter.go.bak`
   - `scripts/stop-all-services.sh.bak`
   - `challenges/data/challenges_bank.json.bak`

## Critical memory/concurrency items for Phase 3
Target files verified to exist at expected paths:
- `internal/ensemble/background/worker_pool.go` — unbounded `pendingResults` sync.Map
- `internal/security/guardrails.go` — unbounded `byGuardrail` sync.Map
- `internal/cache/provider_cache.go` — manual Lock/Unlock without defer
- `internal/cache/cache_service.go` — manual userKeysMu Lock/Unlock without defer

## Existing bin artifacts (for size comparison later)
- `bin/helixagent`: 94,323,488 bytes (Apr 8 22:05)
- `bin/api`: 28,090,633 bytes
- `bin/grpc-server`: 30,617,865 bytes
- `bin/cognee-mock`: 6,164,745 bytes
- `bin/generate-constitution`: 22,905,097 bytes

## Test suite
Full `go test ./...` not run in Phase 0 — requires live infra (PostgreSQL/Redis/Mock
LLM via `./bin/helixagent` boot) which falls outside the baseline-only scope of this
phase. Phase 9 validation will establish the end-state test matrix.

## Non-negotiables for every subsequent phase
- No sudo, no interactive prompts.
- No CI/CD, no Git hooks, no `.github/workflows`.
- No manual container commands — containers orchestrated only via `./bin/helixagent`.
- SSH-only for any Git operation.
- `GOMAXPROCS=2 nice -n 19` on every test/build invocation.
- `go mod vendor` after any submodule touch.
