.claude/*.lock

.env

.opencode
.opencode/*
.opencode/*.*

GIT_PUSH_SUMMARY.txt

# TLS certs (dev) — CONST-053 / §11.4.30: private keys/certs are never
# versioned. Regeneration mechanism (§11.4.77): certs/gen_dev_certs.sh
# (self-signed, CN=localhost, dev-only). See certs/README.md.
# NOTE: intentionally file-glob (*.pem / *.key), not a `certs/` directory
# ignore — a directory-level ignore would also hide the regeneration
# script/doc below it from git, and negation cannot re-include a file
# whose parent directory is itself excluded.
*.pem
*.key
!certs/gen_dev_certs.sh
!certs/README.md

# Built binaries
/helixagent
/api
/grpc-server

# Go basics
bin/
vendor/
vendor.bak/
*.exe
*.test
*.out
*.prof*
coverage/
*.log
.env*
!.env.example
!.env.mcp.example
!.env.mcps.example
!.env.scale.example

# Host-local secret exports sourced from the shell rc — NEVER committed
# (CONST-042 / CONST-053 / §11.4.10). Only the placeholder template is tracked.
api_keys.sh
!api_keys.sh.example
*.swp
*.tmp
*.bak
.env/
.idea/
.vscode/
__pycache__/

# MCP Servers (submodule build artifacts)
mcp_servers/node_modules/
mcp_servers/src/*/dist/
mcp_servers/src/*/node_modules/

# MCP Submodules build artifacts
MCP/submodules/*/node_modules/
MCP/submodules/*/dist/
MCP/submodules/*/.venv/
MCP/submodules/*/__pycache__/
MCP/submodules/*/*.egg-info/

# MCP Plugin build artifacts
plugins/mcp-server/node_modules/
plugins/mcp-server/dist/

# Local MCP server installations
.helixagent/mcp-servers/*/node_modules/

# MCP configuration files with secrets (user-specific)
# Note: Template configs are tracked, user configs with real keys are not
.env.mcps
.env.mcp

# SQLite database runtime files
*.db-shm
*.db-wal
*.db-journal

# Runtime logs
logs/
*.log

# Release build artifacts (binaries only, .version-data is tracked)
releases/helixagent/
releases/api/
releases/grpc-server/
releases/cognee-mock/
releases/sanity-check/
releases/mcp-bridge/
releases/generate-constitution/

# Security scan reports
reports/security/
reports/sbom/
run_sonar.sh

# Phase-boundary metrics snapshots (time-stamped; regenerated per invocation)
reports/metrics-snapshots/
reports/coverage-floor/

# Per-run latency stress output (HXC-165). The committed reference baseline
# reports/latency/p99-baseline-2026-03-16.txt stays tracked and read-only;
# only the per-run measurements below are excluded.
# Regeneration (§11.4.77): pure test output — nothing builds, runs or tests
# against it. Recreate with:
#   go test -run '^TestExtremeP99LatencyBaseline$' ./tests/stress/
reports/latency/runs/

# CI generated reports
reports/go/
reports/mobile/
reports/web/
reports/summary.html
reports/results.json
reports/false-positive-checks.json

# CI caches
.ci-cache/

# Mobile release artifacts
releases/mobile/
releases/web/

# CLI agent local memory/state directories
.junie/
.gemini/

# Video recordings from HelixQA sessions
qa-results/**/*.mp4
qa-results/**/*.webm
qa-results/**/*.mkv
qa-results/**/*.avi
qa-results/**/*.mov
**/*.screenrecord
node_modules/

# Userflow test result output directory
internal/challenges/userflow/results/

releases/*
releases/*.*

!releases/.version-data
!releases/.version-data/*
!releases/.version-data/*.*


# QA Results (ephemeral test output)
qa-results/

# Faulty API Keys tracking file (contains API key names, NOT values)
.faulty_api_keys

# Unsupported API Keys tracking file (keys from .env not recognized as providers)
.unsupported_api_keys

# CLI Agent config backups (may contain API keys)
scripts/cli-agents/configs/backups/
**/*.backup*
**/*backup*.json
/audit
/cognee-mock

# placement plans are runtime artifacts, regenerated each boot
.placement-plan-*.json
.placement-*-*.yml
.placement-*/

# challenge & monitoring run artifacts (per-run, not source state)
challenges/monitoring/reports/html_report_test_*/
challenges/monitoring/reports/json_report_test_*/
challenges/monitoring/reports/report_test_*/
docs/reports/LLM_SCORING_REPORT_*.md

# sqlite memory store created by `bin/helixagent` at runtime
/data/
/internal/router/data/
Website/node_modules/
