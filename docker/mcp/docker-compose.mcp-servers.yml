version: '3.8'

# MCP Servers Docker Compose - Complete Infrastructure
# Builds and runs ALL MCP servers from local git submodules
# These servers expose stdio-based MCP protocol over TCP using socat
#
# Port Ranges:
#   9101-9199: Core MCP servers (from MCP-Servers monorepo)
#   9201-9299: Database MCP servers
#   9301-9399: Vector database MCP servers
#   9401-9499: DevOps/Infrastructure MCP servers
#   9501-9599: Browser automation MCP servers
#   9601-9699: Communication MCP servers
#   9701-9799: Productivity MCP servers
#   9801-9899: Search/AI MCP servers
#   9901-9999: Cloud provider MCP servers

services:
  # =============================================================================
  # Core MCP Servers (from MCP-Servers monorepo) - Ports 9101-9199
  # =============================================================================

  mcp-fetch:
    build:
      context: ../../../../mcp_servers
      dockerfile: ../submodules/helix_agent/docker/mcp/Dockerfile.mcp-server
      args:
        SERVER_NAME: fetch
    container_name: helixagent-mcp-fetch
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_FETCH:-8200}:9000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9000"]
      interval: 10s
      timeout: 5s
      retries: 3

  mcp-git:
    build:
      context: ../../../../mcp_servers
      dockerfile: ../submodules/helix_agent/docker/mcp/Dockerfile.mcp-server
      args:
        SERVER_NAME: git
    container_name: helixagent-mcp-git
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_GIT:-8201}:9000"
    volumes:
      - ${HOME}:/home/user:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9000"]
      interval: 10s
      timeout: 5s
      retries: 3

  mcp-time:
    build:
      context: ../../../../mcp_servers
      dockerfile: ../submodules/helix_agent/docker/mcp/Dockerfile.mcp-server
      args:
        SERVER_NAME: time
    container_name: helixagent-mcp-time
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_TIME:-8202}:9000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9000"]
      interval: 10s
      timeout: 5s
      retries: 3

  mcp-filesystem:
    build:
      context: ../../../../mcp_servers
      dockerfile: ../submodules/helix_agent/docker/mcp/Dockerfile.mcp-server
      args:
        SERVER_NAME: filesystem
    container_name: helixagent-mcp-filesystem
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_FILESYSTEM:-8203}:9000"
    volumes:
      - ${HOME}:/home/user
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9000"]
      interval: 10s
      timeout: 5s
      retries: 3

  mcp-memory:
    build:
      context: ../../../../mcp_servers
      dockerfile: ../submodules/helix_agent/docker/mcp/Dockerfile.mcp-server
      args:
        SERVER_NAME: memory
    container_name: helixagent-mcp-memory
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_MEMORY:-8204}:9000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9000"]
      interval: 10s
      timeout: 5s
      retries: 3

  mcp-everything:
    build:
      context: ../../../../mcp_servers
      dockerfile: ../submodules/helix_agent/docker/mcp/Dockerfile.mcp-server
      args:
        SERVER_NAME: everything
    container_name: helixagent-mcp-everything
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_EVERYTHING:-8205}:9000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9000"]
      interval: 10s
      timeout: 5s
      retries: 3

  mcp-sequentialthinking:
    build:
      context: ../../../../mcp_servers
      dockerfile: ../submodules/helix_agent/docker/mcp/Dockerfile.mcp-server
      args:
        SERVER_NAME: sequentialthinking
    container_name: helixagent-mcp-sequentialthinking
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_SEQUENTIAL_THINKING:-8206}:9000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "9000"]
      interval: 10s
      timeout: 5s
      retries: 3

  # =============================================================================
  # Database MCP Servers - Ports 9201-9299
  # =============================================================================

  mcp-redis:
    build:
      context: ../../MCP/submodules/redis-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-python
    container_name: helixagent-mcp-redis-server
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_MONGODB:-8210}:9000"
    environment:
      - REDIS_URL=redis://:helixagent123@mcp-redis-backend:6379
    depends_on:
      - mcp-redis-backend
    restart: unless-stopped

  mcp-redis-backend:
    image: redis:7-alpine
    container_name: helixagent-mcp-redis-backend
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_REDIS_MCP:-8110}:6379"
    command: redis-server --requirepass helixagent123
    volumes:
      - mcp-redis-data:/data
    restart: unless-stopped

  mcp-mongodb:
    build:
      context: ../../MCP/submodules/mongodb-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-mongodb-server
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_REDIS:-8211}:9000"
    environment:
      - MONGODB_URI=mongodb://helixagent:helixagent123@mcp-mongodb-backend:27017/helixagent?authSource=admin
    depends_on:
      - mcp-mongodb-backend
    restart: unless-stopped

  mcp-mongodb-backend:
    image: mongo:7
    container_name: helixagent-mcp-mongodb-backend
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=helixagent
      - MONGO_INITDB_ROOT_PASSWORD=helixagent123
    volumes:
      - mcp-mongodb-data:/data/db
    restart: unless-stopped

  mcp-supabase:
    # Per-submodule Dockerfile (BUGFIXES Issue #54): pnpm monorepo;
    # uses packages/mcp-server-supabase's bin → dist/transports/stdio.js
    # entry. Verified: initialize → serverInfo "supabase" v0.7.0.
    build:
      context: ../../MCP/submodules/supabase-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-supabase
    container_name: helixagent-mcp-supabase
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_MYSQL:-8212}:9000"
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
    restart: unless-stopped

  # =============================================================================
  # Vector Database MCP Servers - Ports 9301-9399
  # =============================================================================

  mcp-qdrant:
    # Per-submodule Dockerfile (BUGFIXES Issue #54): qdrant-mcp is a
    # PyPI package (mcp-server-qdrant); generic mcp-python heuristics
    # couldn't find it. Dockerfile.mcp-qdrant uv-installs it and
    # invokes via stdio transport.
    build:
      context: ../../MCP/submodules/qdrant-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-qdrant
    container_name: helixagent-mcp-qdrant-server
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_QDRANT:-8215}:9000"
    environment:
      - QDRANT_URL=http://mcp-qdrant-backend:6335
    depends_on:
      - mcp-qdrant-backend
    restart: unless-stopped

  mcp-qdrant-backend:
    image: qdrant/qdrant:latest
    container_name: helixagent-mcp-qdrant-backend
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "6335:6333"
      - "6336:6334"
    volumes:
      - mcp-qdrant-data:/qdrant/storage
    restart: unless-stopped

  # =============================================================================
  # DevOps/Infrastructure MCP Servers - Ports 9401-9499
  # =============================================================================

  mcp-kubernetes:
    build:
      context: ../../MCP/submodules/kubernetes-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-go
    container_name: helixagent-mcp-kubernetes
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_GITHUB:-8220}:9000"
    volumes:
      - ${HOME}/.kube:/root/.kube:ro
    restart: unless-stopped

  mcp-github:
    build:
      context: ../../MCP/submodules/github-mcp-server
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-go
    container_name: helixagent-mcp-github
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_GITLAB:-8221}:9000"
    environment:
      - GITHUB_PERSONAL_ACCESS_TOKEN=${GITHUB_TOKEN}
    restart: unless-stopped

  mcp-cloudflare:
    profiles:
      # Broken under current generic Dockerfile.mcp-{submodule,python}
      # — each submodule has a bespoke build/launch (BUGFIXES Issue #54).
      # Opt-in: docker compose --profile submodule-experimental up -d
      - submodule-experimental
    build:
      context: ../../MCP/submodules/cloudflare-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-cloudflare
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_SENTRY:-8222}:9000"
    environment:
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN}
      - CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID}
    restart: unless-stopped

  mcp-heroku:
    build:
      context: ../../MCP/submodules/heroku-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-heroku
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_KUBERNETES:-8223}:9000"
    environment:
      - HEROKU_API_KEY=${HEROKU_API_KEY}
    restart: unless-stopped

  mcp-sentry:
    # Per-submodule Dockerfile (BUGFIXES Issue #54): turbo monorepo;
    # uses packages/mcp-server's bin → dist/index.js. NOTE: requires
    # SENTRY_ACCESS_TOKEN env var or it exits at startup. Verified
    # with dummy token: initialize → serverInfo "Sentry MCP" v0.29.0.
    build:
      context: ../../MCP/submodules/sentry-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-sentry
    container_name: helixagent-mcp-sentry
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_DOCKER:-8224}:9000"
    environment:
      - SENTRY_AUTH_TOKEN=${SENTRY_AUTH_TOKEN}
      - SENTRY_ORG=${SENTRY_ORG}
    restart: unless-stopped

  # =============================================================================
  # Browser Automation MCP Servers - Ports 9501-9599
  # =============================================================================

  mcp-playwright:
    # Per-submodule Dockerfile (BUGFIXES Issue #54): playwright-mcp
    # has a multi-stage browser build + ENTRYPOINT
    # ["node", "cli.js", ...]. Dockerfile.mcp-playwright replicates
    # that with socat wrapping for stdio→TCP. CRITICAL: raw pipes,
    # NOT pty (pty corrupts NDJSON framing).
    build:
      context: ../../MCP/submodules/playwright-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-playwright
    container_name: helixagent-mcp-playwright
    mem_limit: 3g
    memswap_limit: 3g
    pids_limit: 4096
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_PLAYWRIGHT:-8234}:9000"
    restart: unless-stopped

  mcp-browserbase:
    build:
      context: ../../MCP/submodules/browserbase-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-browserbase
    mem_limit: 3g
    memswap_limit: 3g
    pids_limit: 4096
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_BROWSERBASE:-8235}:9000"
    environment:
      - BROWSERBASE_API_KEY=${BROWSERBASE_API_KEY}
      - BROWSERBASE_PROJECT_ID=${BROWSERBASE_PROJECT_ID}
    restart: unless-stopped

  mcp-firecrawl:
    build:
      context: ../../MCP/submodules/firecrawl-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-firecrawl
    mem_limit: 2g
    memswap_limit: 2g
    pids_limit: 2048
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_FIRECRAWL:-8236}:9000"
    environment:
      - FIRECRAWL_API_KEY=${FIRECRAWL_API_KEY}
    restart: unless-stopped

  # =============================================================================
  # Communication MCP Servers - Ports 9601-9699
  # =============================================================================

  mcp-slack:
    build:
      context: ../../MCP/submodules/slack-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-go
    container_name: helixagent-mcp-slack
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_SLACK:-8238}:9000"
    environment:
      - SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN}
      - SLACK_TEAM_ID=${SLACK_TEAM_ID}
    restart: unless-stopped

  mcp-telegram:
    build:
      context: ../../MCP/submodules/telegram-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-python
    container_name: helixagent-mcp-telegram
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_DISCORD:-8239}:9000"
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    restart: unless-stopped

  # =============================================================================
  # Productivity MCP Servers - Ports 9701-9799
  # =============================================================================

  mcp-notion:
    # Per-submodule Dockerfile (BUGFIXES Issue #54): notion installs
    # as a global npm binary; generic Dockerfile.mcp-submodule
    # heuristics couldn't find dist/index.js. Dockerfile.mcp-notion
    # replicates upstream's npm-link approach + socat wrapper.
    build:
      context: ../../MCP/submodules/notion-mcp-server
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-notion
    container_name: helixagent-mcp-notion
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_NOTION:-8250}:9000"
    environment:
      - NOTION_API_KEY=${NOTION_API_KEY}
    restart: unless-stopped

  mcp-trello:
    build:
      context: ../../MCP/submodules/trello-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-trello
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_LINEAR:-8251}:9000"
    environment:
      - TRELLO_API_KEY=${TRELLO_API_KEY}
      - TRELLO_TOKEN=${TRELLO_TOKEN}
    restart: unless-stopped

  mcp-airtable:
    build:
      context: ../../MCP/submodules/airtable-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-airtable
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_JIRA:-8252}:9000"
    environment:
      - AIRTABLE_API_KEY=${AIRTABLE_API_KEY}
    restart: unless-stopped

  mcp-obsidian:
    build:
      context: ../../MCP/submodules/obsidian-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-obsidian
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_ASANA:-8253}:9000"
    volumes:
      - ${HOME}/Obsidian:/vault:ro
    restart: unless-stopped

  mcp-atlassian:
    # Per-submodule Dockerfile (BUGFIXES Issue #54): atlassian-mcp
    # is a PyPI uv-installed binary `mcp-atlassian`; needs explicit
    # `--transport stdio` flag. Dockerfile.mcp-atlassian replicates
    # upstream's two-stage uv build + socat wrapper.
    build:
      context: ../../MCP/submodules/atlassian-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-atlassian
    container_name: helixagent-mcp-atlassian
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_TRELLO:-8254}:9000"
    environment:
      - ATLASSIAN_URL=${ATLASSIAN_URL}
      - ATLASSIAN_EMAIL=${ATLASSIAN_EMAIL}
      - ATLASSIAN_API_TOKEN=${ATLASSIAN_API_TOKEN}
    restart: unless-stopped

  # =============================================================================
  # Search/AI MCP Servers - Ports 9801-9899
  # =============================================================================

  mcp-brave-search:
    build:
      context: ../../MCP/submodules/brave-search
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-brave-search
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_BRAVE_SEARCH:-8260}:9000"
    environment:
      - BRAVE_API_KEY=${BRAVE_API_KEY}
    restart: unless-stopped

  mcp-perplexity:
    build:
      context: ../../MCP/submodules/perplexity-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-perplexity
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_EXA:-8261}:9000"
    environment:
      - PERPLEXITY_API_KEY=${PERPLEXITY_API_KEY}
    restart: unless-stopped

  mcp-omnisearch:
    build:
      context: ../../MCP/submodules/omnisearch-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-omnisearch
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_TAVILY:-8262}:9000"
    restart: unless-stopped

  mcp-context7:
    # Per-submodule Dockerfile (BUGFIXES Issue #54): pnpm monorepo;
    # uses packages/mcp's bin → dist/index.js, default stdio
    # transport. Verified: initialize → serverInfo "Context7" v2.1.4.
    build:
      context: ../../MCP/submodules/context7-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-context7
    container_name: helixagent-mcp-context7
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_PERPLEXITY:-8263}:9000"
    restart: unless-stopped

  mcp-llamaindex:
    profiles:
      # Broken under current generic Dockerfile.mcp-{submodule,python}
      # — each submodule has a bespoke build/launch (BUGFIXES Issue #54).
      # Opt-in: docker compose --profile submodule-experimental up -d
      - submodule-experimental
    build:
      context: ../../MCP/submodules/llamaindex-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-llamaindex
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_KAGI:-8264}:9000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped

  # =============================================================================
  # Cloud Provider MCP Servers - Ports 9901-9999
  # =============================================================================

  mcp-workers:
    build:
      context: ../../MCP/submodules/workers-mcp
      dockerfile: ../../../docker/mcp/Dockerfile.mcp-submodule
    container_name: helixagent-mcp-workers
    mem_limit: 1g
    memswap_limit: 1g
    pids_limit: 1024
    oom_score_adj: 500
    ports:
      - "${HELIXAGENT_PORT_MCP_GDRIVE:-8270}:9000"
    environment:
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN}
      - CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID}
    restart: unless-stopped

volumes:
  mcp-redis-data:
  mcp-mongodb-data:
  mcp-qdrant-data:

networks:
  default:
    name: helixagent-mcp-network
